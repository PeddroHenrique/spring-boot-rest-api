# Nome do banco, usuario e senha
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myblog
    username: root
    password: root

    # Driver jdbc
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Hibernate e JPA
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

app:
  jwt-secret: ee5cd7d5d96c8874117891b2c92a036f96918e66c102bc698ae77542c186f981

app-jwt-expiration-milliseconds: 604800000
